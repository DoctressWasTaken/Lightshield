version: '3.7'
services:
  ### Databases
  buffer_db:
    image: redis:6.0
    command: [ "redis-server", "--appendonly", "yes" ]
    volumes:
      - redis:/data
    networks:
      default:
        aliases:
          - redis
    restart: always

volumes:
  redis:
    name: ${SERVER}_lightshield_buffer

networks:
  default:
    external:
      name: lightshield
