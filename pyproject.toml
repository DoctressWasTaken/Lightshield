[tool.poetry]
name = "Lightshield"
version = "2.0.0"
description = "Datapipeline for the Riot REST API utilizing microservices for a modular structure."
authors = ["Doctress"]
license = "Apache 2.0"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
black = "^20.8b1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
