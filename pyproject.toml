[tool.poetry]
name = "lightshield"
version = "0.9.0"
description = "Riot REST API automatic pipeline."
authors = ["Robin Holzwarth <robinholzwarth95@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
docker = "^4.4.4"

[tool.poetry.dev-dependencies]
black = "^20.8b1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
